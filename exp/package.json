{
  "name": "exp",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "esbuild --bundle --watch src/index.ts --outfile=public/bundle.js",
    "upload": "surge public",
    "test": "tsc --noEmit",
    "clean": "rimraf dist",
    "build": "rimraf dist && tsc -p . && rimraf dist/tsconfig.tsbuildinfo",
    "tsc": "tsc -p ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^24.0.14",
    "esbuild": "^0.25.6",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "surge": "^0.24.6",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  },
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
